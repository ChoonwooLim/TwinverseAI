# 버그수정 로그

> 이 문서는 /end 스킬 호출 시 자동 업데이트됩니다.

| 날짜 | 버그 설명 | 원인 | 수정 내용 | 관련 파일 |
|------|----------|------|----------|----------|
| 2026-04-04 | 로그인 후 다시 로그인 화면 표시 | 401 인터셉터가 로그인 API 응답까지 토큰 삭제 처리 | 인터셉터에서 auth 경로 제외 + ProtectedRoute 로직 개선 | frontend/src/services/api.js, ProtectedRoute.jsx |
| 2026-04-04 | JWT 토큰 디코드 실패 | python-jose가 sub 클레임을 문자열로 기대하는데 정수 전달 | create_access_token에서 sub를 str()로 변환 | backend/services/auth_service.py, backend/routers/auth.py |
| 2026-04-05 | TVDesk NPC 호출/대화 "연결끊김" | DeskRPG 쿠키 Secure 플래그 → Cloudflare Tunnel 내부 HTTP에서 브라우저가 쿠키 저장 거부 | COOKIE_SECURE=false 환경변수 설정 | 서버: ~/.deskrpg/start.sh |
| 2026-04-05 | 리눅스 브라우저 소켓 인증 실패 | DeskRPG token 쿠키 미설정, twinverse_token만 존재 | 프록시에서 twinverse_token → DeskRPG JWT 자동 변환/주입 | 서버: ~/.deskrpg/proxy.js |
| 2026-04-06 | Docker 배포 시 프로젝트 문서 404 | Path(__file__).parent.parent.parent가 Docker에서 /docs로 해석 | DOCS_DIR 환경변수 + 자동 탐색 로직, 이후 PostgreSQL 기반으로 전면 전환 | backend/routers/docs.py, backend/models/document.py, backend/main.py |
| 2026-04-06 | 갤러리 이미지 배포 404 (1차) | .gitignore에 uploads/ → Docker 빌드 시 COPY 실패 → 502 | .gitignore에서 gallery-*.jpg 예외 처리 + git add -f | .gitignore, uploads/gallery-*.jpg |
| 2026-04-06 | 갤러리 이미지 배포 404 (2차) | Docker VOLUME /app/uploads가 빈 볼륨으로 마운트 → COPY한 이미지 덮어씀 | gallery_defaults/를 backend/ 내부에 포함, 시작 시 uploads로 복사 | backend/gallery_defaults/, backend/main.py, Dockerfile |
| 2026-04-06 | UPLOAD_DIR 빈 문자열로 CWD 해석 | Orbitron이 UPLOAD_DIR=""로 override → Path("")=CWD(.) → 소스 폴더를 uploads로 착각 | .strip() 후 빈 문자열이면 fallback 경로 사용 | backend/main.py, backend/routers/files.py |
| 2026-04-06 | StaticFiles mount Docker VOLUME 충돌 | StaticFiles mount가 VOLUME 마운트된 빈 디렉토리 참조 → 파일 미검색 | StaticFiles mount 제거 → 명시적 @app.get("/uploads/{filename}") 라우트 사용 | backend/main.py |
| 2026-04-06 | 로컬 갤러리 이미지 깨짐 | Vite 프록시 미설정 → img src="/uploads/..." 요청이 SPA HTML(text/html) 반환 | vite.config.js에 /api, /uploads, /health 프록시 추가 | frontend/vite.config.js |
| 2026-04-09 | TVDeskRun 2D 가상오피스 페이지 사라짐 | PS2 DeskLaunch로 덮어씀 | TVDeskRun 원래 페이지 복원, DeskLaunch 별도 라우트 | TVDeskRun.jsx, DeskLaunch.jsx |
| 2026-04-09 | subprocess.CREATE_NEW_PROCESS_GROUP Linux 오류 | Windows 전용 플래그를 Linux에서 사용 | os.name 체크로 Windows 전용 분기 | ps2_service.py |
| 2026-04-09 | PS2 API URL 프로덕션 빌드 미반영 | .env.production 미설정 | .env.production 추가 + fallback 하드코딩 | .env.production, ps2api.js |
| 2026-04-09 | 맵 선택해도 같은 맵 로딩 (UE5 패키지 빌드) | 패키지 빌드가 positional map arg, ExecCmds 무시 | GameInstance::OnStart()에서 -MapOverride 파싱 → OpenLevel() | TwinverseDeskGameInstance.cpp |
| 2026-04-09 | spawn이 기존 세션 반환 (다른 맵인데도) | spawn_session idempotent 로직이 맵 비교 안 함 | PS2Session에 map_path 필드 추가, 맵 다르면 기존 종료 후 재생성 | ps2_service.py, ps2_session.py |
| 2026-04-09 | PCG 이주 레벨에서 클래식카 로드 불가 | ChaosVehiclesPlugin 미활성화 + 에셋 폴더 누락 | .uproject에 ChaosVehiclesPlugin + PCG 플러그인 5종 추가 | TwinverseDesk.uproject |
| 2026-04-09 | ABP_Dashboard 컴파일 에러 (VehicleMovementComponent) | Parent Class가 AnimInstance여서 차량 함수 없음 | Parent Class → VehicleAnimationInstance + Cast 노드 재생성 | ABP_Dashboard.uasset |
| 2026-04-09 | UE5 패키징 실패 C4458 | 로컬 변수 Mesh가 ACharacter::Mesh 멤버 숨김 | Mesh → CharMesh로 변수명 변경 | OfficeCharacter.cpp, OfficeNPC.cpp |
| 2026-04-10 | Orbitron 배포 시 크래시 루프 | lifespan 시작 작업 중 하나 실패 시 컨테이너 전체 종료, 재시작 반복 | 모든 시작 작업 try-catch로 감싸 개별 실패에도 서버 기동 + Dockerfile에 SECRET_KEY/DATABASE_URL 기본값 + HEALTHCHECK | Dockerfile, backend/main.py, backend/database.py |
| 2026-04-10 | DeskLaunch "플랫폼 연결오류" — 브라우저 CORS 에러로 보임 | GPU PC 백엔드가 .env의 한글 주석 때문에 기동 실패. slowapi Limiter가 starlette Config로 .env 로드 시 Windows 기본 cp949로 읽어 UnicodeDecodeError → uvicorn 크래시 → 서버 응답 없음 → preflight 실패를 브라우저가 CORS 오류로 표시. Cloudflare Tunnel은 정상(502 반환), CORS 코드도 이미 정상. 근본 원인은 서버 프로세스 사망 | (1) backend/.env의 한글 주석 2줄 제거(.env 규칙 위반이기도 함) (2) scripts/start_gpu_server.bat uvicorn 실행 전 `set PYTHONUTF8=1` 추가 — 앞으로 UTF-8 바이트 다시 들어와도 Python 전역 UTF-8 모드로 안전 | backend/.env, scripts/start_gpu_server.bat |
| 2026-04-10 | DeskLaunch "Invalid token" 401 — CORS 해결 직후 | 브라우저 localStorage의 JWT가 과거 SECRET_KEY 시기(ef6784c 이전)에 발급됐거나 8시간 만료. Orbitron/GPU PC 양쪽 SECRET_KEY 동일(`orbitron-twinverseai-secret-key-2026`) 확인, Orbitron이 방금 발급한 토큰은 GPU PC 로컬/터널 양쪽 HTTP 200. 단순 stale token. 재발 방지: ps2api.js에 response interceptor 부재 → 401 와도 자동 로그아웃 없음 (api.js에만 존재) | frontend/src/services/ps2api.js에 api.js와 동일한 401 interceptor 추가 (localStorage clear + /login 리다이렉트). 사용자는 로그아웃/재로그인으로 즉시 해결 | frontend/src/services/ps2api.js |
| 2026-04-12 | 2026-04-10 cp949 인시던트 재발 방지 하드닝 | 당일 수정(.env 한글 주석 제거 + PYTHONUTF8=1)은 됐지만 방어선이 1개뿐이었음. 새 한글 주석이 들어오거나 PYTHONUTF8 미설정 환경에선 다시 crash 가능. 또 silent crash를 사전에 포착할 수단 부재 | 3중 방어: (1) load_dotenv 4곳에 encoding="utf-8" 명시 (2) scripts/check_backend_ready.py로 .env 바이트 직접 검증 + backend.main import 테스트 + /health 폴링 (3) start_gpu_server.bat가 pre-flight 실패 시 uvicorn 띄우지 않고 fail-loud | backend/{main,ps2_server,seed_admin}.py, backend/alembic/env.py, scripts/check_backend_ready.py, scripts/start_gpu_server.bat, CLAUDE.md |
| 2026-04-12 | DeskLaunch 3D Virtual Office 카드 실행 불가인데 클릭됨 | OfficeMain.umap + Dedicated Server 미구축 상태인데 카드 UI는 정상 레벨처럼 표시 → 사용자가 클릭해도 실패만 반복 | LEVELS[0]에 disabled/statusLabel/statusNote 필드 추가, 기본 selectedLevel을 non-disabled 첫 레벨로, handleSpawn 가드, 카드에 "개발중 / 실행 불가" 오버레이 배지 + 그레이스케일 필터 | frontend/src/pages/twinversedesk/DeskLaunch.jsx, DeskLaunch.module.css |
| 2026-04-12 | DeskLaunch 헬스체크 실패가 silent — 사용자가 이유 모름 | `catch { /* spawner unavailable */ }`로 에러를 삼켜서 2026-04-10처럼 "왜 안 되지" 상황 재현 위험 | healthError state + visible offlineBanner UI. HTTP 상태/네트워크 오류를 구체 문구로 표시, 401은 interceptor가 처리하므로 배너 스킵 | frontend/src/pages/twinversedesk/DeskLaunch.jsx, DeskLaunch.module.css |
| 2026-04-16 | OpenClaw `agents list` 500 오류 (CLI 래퍼 문법 오판) | `openclaw agents --list` 같은 잘못된 인자로 호출해 CLI 가 usage 에러 반환 | 실제 CLI 문법 `openclaw agents list --json` 으로 정정, 기타 서브커맨드도 실문법 확인 후 매핑 | backend/services/openclaw_cli.py |
| 2026-04-16 | OpenClaw SSH 인증 실패 | password 기반으로 시도했지만 서버는 publickey 전용 | paramiko 를 publickey(`~/.ssh/id_ed25519`) 기반으로 전환, 에러 메시지 정리 | backend/services/openclaw_ssh.py |
| 2026-04-16 | OpenClaw 채팅 WS 연결 실패 | `websockets.connect` 의 imports 경로가 버전별로 다름 (legacy vs asyncio) → 런타임 AttributeError | `websockets.asyncio.client.connect` 를 우선 import 하고 legacy 로 폴백 | backend/routers/admin_openclaw_console.py |
| 2026-04-16 | admin 모델 선택 드롭다운이 흰 바탕 위 흰 글씨 | 다크 테마 전역 style 이 `<select>` 네이티브 위젯에 미적용 | `select.input` 전용 dark theme override (background `#1a1a1a`, caret svg) | frontend/src/pages/admin/AdminOpenClawConsole.module.css |
| 2026-04-16 | OpenClaw WS 요청이 토큰 누락으로 거부 | admin WS 엔드포인트가 Bearer 토큰 요구인데 프런트가 쿼리 파라미터로 전달 | ws_auth util 로 쿼리 파라미터/헤더 양쪽 허용, 파일 읽기 폴백 포함, WS 공개 엔드포인트 보강 | backend/utils/ws_auth.py, backend/routers/admin_openclaw_console.py |
| 2026-04-16 | Gateway 가 프런트 프레임을 무시 (RPC 응답 없음) | 관리자 릴레이가 `{jsonrpc, id, method}` 로 보내는데 gateway 는 네이티브 `{type:"req"/"res"/"event"}` 프레임만 인식 | 릴레이가 gateway 네이티브 프레임 형식으로 encode/decode 하도록 재작성 | backend/routers/admin_openclaw_console.py |
| 2026-04-16 | gateway.connect 에서 scope 부족 → agents.* 거부 | 5 개 operator scope 를 요청하지 않아 `scope_missing` | connect 핸드셰이크에서 5 operator scope 전체 요청 | backend/routers/admin_openclaw_console.py |
| 2026-04-16 | `dangerouslyDisableDeviceAuth` 가 scope 를 강제로 비워버림 | Non-TUI 클라이언트로 식별되면 안전망으로 scope 박탈 | 클라이언트를 TUI 로 식별하도록 user-agent/클라이언트 필드 세팅 → scope 유지 | backend/routers/admin_openclaw_console.py |
| 2026-04-16 | relay 응답이 항상 undefined | gateway 는 `{payload: ...}` 로 싸서 보내는데 릴레이가 `result` 키를 읽음 | unwrap 시 `payload` 우선 (없으면 `result` 폴백) | backend/routers/admin_openclaw_console.py |
| 2026-04-16 | 채팅 메시지가 수신되지 않음 | `sessions.messages.*` 이벤트를 리슨했지만 gateway 는 `session.message` 발행 | 채팅 event 리스너를 `session.message` 로 변경 | frontend/src/pages/admin/openclaw/ChatTab.jsx, backend/routers/admin_openclaw_console.py |
| 2026-04-16 | 새 에이전트 생성 후 gateway 가 IDENTITY 읽기 실패 (EACCES) | agents add 시 생성된 디렉터리가 root 소유 → node 프로세스 거부 | agents.add 직후 `chown -R node:node /srv/openclaw/data/agents/<id>` 자동 실행 | backend/services/openclaw_cli.py |
| 2026-04-16 | `agents.update` RPC 가 `id` 를 거부 | 스키마가 `agentId` 만 받음 | 모든 agents.update 경로에서 `agentId` 로 전달 | backend/routers/admin_openclaw_console.py, frontend/src/pages/admin/openclaw/AgentsTab.jsx |
| 2026-04-16 | 한 에이전트 모델 변경 시 다른 에이전트 설정 전체 wipe + 히스토리 섞임 | 업데이트 payload 가 전체 agents 배열을 덮어씀 + 채팅 히스토리를 단일 state 로 관리 | patch 방식 업데이트 (agentId 기반) + `messagesByAgent` 맵으로 per-agent 히스토리 분리 | frontend/src/pages/admin/openclaw/{AgentsTab,ChatTab}.jsx |
| 2026-04-16 | TwinverseDesk 실행 시 자동 로그아웃 | api.js response interceptor 가 401 에서 localStorage 를 비움 → PS2 헬스체크 401(의도된 거부)도 삭제 트리거 | PS2 헬스체크 경로는 interceptor 에서 스킵 (401 이어도 토큰 보존) | frontend/src/services/api.js |
| 2026-04-18 | 채팅 탭 502 (agents 목록 N+1) | 에이전트마다 `files.get` 으로 IDENTITY.md 를 별도 페치 → SSH 폭주 + 타임아웃 | 서버 응답에 description 포함, 프론트는 한 번의 list 호출만 | backend/routers/admin_openclaw_console.py, backend/services/openclaw_cli.py, frontend/src/pages/admin/openclaw/ChatTab.jsx |
| 2026-04-18 | IDENTITY.md 설명에 "한국어로만 답한다" 같은 지시문 노출 | 첫 줄 단순 추출이라 템플릿 주석/한국어 지시 블록까지 포함 | `## 역할` 섹션 1 순위 + `Role:`/`역할:` 라벨 폴백 + 지시문 영역 regex 배제 | backend/services/openclaw_cli.py |
| 2026-04-18 | 어시스턴트 답변이 두 번 렌더링 | gateway `chat` final → `session.message` alias 의 중복 emit | 서버측 `emitted_message_ids` 세트로 dedupe | backend/routers/admin_openclaw_console.py |
| 2026-04-18 | 에이전트 모델만 바꿨을 때 기존 세션이 과거 모델로 동작 | 채팅 탭이 세션/WS 캐시를 재사용 | `openclaw:agent-model-changed` CustomEvent dispatch → ChatTab 이 세션/WS 재생성 | frontend/src/pages/admin/openclaw/{AgentsTab,ChatTab}.jsx |
| 2026-04-18 | TwinverseDesk 실행 401 자동 로그아웃 (재발) | ps2api.js 가 여전히 공유 api 인스턴스로 헬스체크 | ps2api.js 경로를 interceptor bypass 규칙에 맞도록 수정 | frontend/src/services/ps2api.js |
| 2026-04-18 | NPC 채팅 — Anthropic API 가 연속된 같은 role 거부 + fallback 모델 구식 | 메시지 배열에 user/assistant 가 2 연속 올 수 있음 | 연속 role 자동 병합 유틸 추가 + fallback 모델 ID 를 최신 Claude 로 교정 | backend/routers/npc.py |
| 2026-04-18 | `docker restart openclaw` 후 `unauthorized: token mismatch` | gateway 가 `auth.token` 을 재생성 | `POST /token/reset` 엔드포인트 + `/app/data/.openclaw_token_override` 볼륨 파일 + 채팅 탭 "토큰 리셋" 버튼 | backend/routers/admin_openclaw_console.py, frontend/src/pages/admin/openclaw/ChatTab.jsx |
| 2026-04-18 | claude-max 응답이 중국어/한국어 혼용 + 가짜 상태 메시지 | Claude Max OAuth access token 8 h 만료 + `auth-profiles.json` 에 STALE 토큰 잔존 | 컨테이너 TTY 로 `claude login` 재실행 후 4 에이전트 auth-profiles 를 fresh `/data/.claude/.credentials.json` 로 싱크 + `managedBy:"claude-cli"` 태그 | /data/.openclaw/agents/{claude-max,claude-opus,main,codex-pro}/agent/auth-profiles.json |
| 2026-04-18 | DeskRPG NPC 고용이 "1/3 연결 중" 또는 "네트워크 오류" 로 실패 | OpenClaw 2026.4.12 reload manager 가 `agents.create` 직후 `plugins.entries.*.config` JSON 재직렬화 diff 오탐 → `SIGUSR1` 전체 재시작 → 이어지는 `agents.files.set` RPC 가 WS 502/RPC timeout 으로 중단 | `/_internal/rpc` 핸들러에 `rpcWithRetry` 재시도 로직 주입 (일시적 WS 오류 감지 → 캐시 무효화 + 3.5s/5s 백오프 × 3 회). **핫패치 적용, 재검증 미완 — 내일 확인** | deskrpg-master/server.js (gitignored local), Orbitron `~/.npm/_npx/.../deskrpg/server.js` (hotpatch + .bak), scripts/patch_deskrpg_rpc_retry.js |
| 2026-04-24 | OpenClaw 에서 의도치 않은 Anthropic API 비용 발생 | LAN OpenClaw config 에 Anthropic provider `apiKey` 가 남아있고 `main` 이 `anthropic/claude-opus-4-7` 로 지정되어 API 호출 가능 상태였음 | Anthropic API key 제거, Anthropic plugin 비활성화, `main`/기본 모델을 `openai-codex/gpt-5.5` 로 전환, API 과금 에이전트 `claude-opus` 는 `agents.list` 에서 격리 | Orbitron.yaml, backend/routers/npc_agent.py, twinverse-ai `/srv/openclaw/data/.openclaw/openclaw.json` |
| 2026-04-24 | OpenClaw config reload skip | 직접 config 복구 중 schema 에 없는 `models.providers.anthropic.enabled`, `agents.list[].displayName` 키가 들어감 | 스키마 미지원 키 제거 후 `openclaw agents list --json` 과 `config get` 으로 정상 검증 | twinverse-ai `/srv/openclaw/data/.openclaw/openclaw.json` |
| 2026-04-24 | DeskRPG NPC 대화에서 "태스크 등록" 응답 후 태스크 수가 0 으로 남음 | PostgreSQL timestamp 컬럼에 ISO 문자열을 전달해 `value.toISOString is not a function` 예외가 발생했고, 런타임 task schema 에 일부 컬럼이 누락됨 | PostgreSQL 은 `Date`, 레거시 SQLite 경로는 ISO 문자열을 쓰도록 timestamp 변환 분기 추가 + task schema 컬럼 동기화 + 라이브 PostgreSQL create/delete smoke test 통과 | deskrpg-master/src/lib/task-manager.js, deskrpg-master/src/db/server-db.js, Orbitron deskrpg package |
| 2026-04-24 | DeskRPG `/characters` 화면이 "로딩 중..." 에 멈춤 | `npm pack` 산출물의 고정 mtime 과 동일 파일 크기 때문에 `rsync` 가 변경된 Next.js route chunk 를 건너뛰어 `/api/auth/status` 500 발생 | Orbitron package 를 `rsync --checksum` 으로 재동기화하고 서버 재시작, `/api/auth/status` 200 및 `/game` 정상 확인 | Orbitron deskrpg package `.next/server/**`, 배포 동기화 절차 |
| 2026-04-29 | `https://twinverseai.twinverse.org` 502 Bad Gateway (Orbitron 재배포 직후) | Orbitron 이 `PORT=3441` env 주입+호스트 포트 3441→컨테이너 3441 매핑하는데, Dockerfile CMD 가 `--port 8000` 하드코딩 → 앱이 8000 에서 listen, 컨테이너 3441 은 비어있음 → nginx upstream connection refused | Dockerfile CMD/HEALTHCHECK 를 `${PORT:-8000}` 사용하도록 변경 (commit 316f2e3). 푸시 후 새 컨테이너 30 초 만에 배포되어 HTTP 200 복구 | Dockerfile |
| 2026-04-29 | OpenClaw `TRAEFIK_HOST` 적용 후 어드민 콘솔 "Gateway 응답 없음" | 부트스트랩이 `gateway.trustedProxies` 와 controlUi dangerous 플래그를 함께 삭제 → device 인증 ON + wrapper-loopback 연결을 외부 클라이언트로 분류 → TwinverseAI 백엔드 (Python websockets) 가 controlUi device 페어링 강요당해 1008 close. Python 클라는 페어링 불가능 | `TRAEFIK_HOST` 환경변수 제거 후 컨테이너 재생성. 보안 critical 3 개는 다른 접근으로 재시도 (백엔드 RPC 경로 연결 또는 부트스트랩 패치) | twinverse-ai openclaw 컨테이너 env, memory `reference_openclaw_traefik_host.md` |
| 2026-05-01 | DeskRPG (`tvdesk.twinverse.org`) 멀티유저 회귀 — 같은 방에서 상대 캐릭터 안 보임 + admin 콘솔 AI 연결 패널 "게이트웨이 설정 로딩중..." 에서 stuck (윈도우 admin 은 캐시된 token 으로 일시 작동, Linux limp2004/sodam2025 는 즉시 실패) | `~/.deskrpg/proxy.js` 의 `USER_MAP` 이 stale: TwinverseAI username `admin` 대신 오타 `admintop` 으로 등록되어 있었고, 새 사용자 `sodam2025hl` 은 아예 누락. proxy 가 매핑 못 찾으면 `token` 쿠키 주입을 silent skip → DeskRPG socket.io 가 인증 없는 upgrade 즉시 reject → 브라우저는 `WebSocket closed before connection established` | (1) `USER_MAP` 에 `admin`/`limp2004`/`sodam2025hl` 3 개 정확 entry 로 교체, (2) 누락 매핑 시 `console.error` 로 사용자명·현재 매핑 키 목록을 출력하도록 개선 (silent → loud), (3) infrastructure repo 에 commit (`d1fd44a` ChoonwooLim/orbitron-infrastructure). 인접 트리거: 같은 시간대 OpenClaw `agents.delete` 가 SIGUSR1 게이트웨이 재시작을 동반해 모든 chat WS 가 한꺼번에 끊김 — `admin-openclaw-console.md` 표에 caveat 추가 | `~/.deskrpg/proxy.js`, `~/WORK/infrastructure/deskrpg/proxy.js`, memory `reference_deskrpg_user_map.md` |
| 2026-05-01 | DeskRPG NPC 가 태스크 완료/진척 보고 시 `[TaskManager] Error handling task action: TypeError: value.toISOString is not a function` 반복 → 보고가 DB 에 안 들어감 | `src/lib/task-reporting.ts:274` 의 `buildQueuedReportRow` 가 `createdAt: nowIso()` (ISO 문자열) 으로 row 를 만들어 PostgreSQL `npcReports.createdAt` (timestamp 컬럼) 에 insert. drizzle ORM 직렬화에서 `.toISOString()` 호출 → string 에 메서드 없음 → 에러. 4-24 fix 가 `task-manager.js` 의 동일 패턴은 고쳤는데 `task-reporting.ts` 는 누락. **추가 발견 (같은 날 후속)**: 같은 파일 line 351 `markReportDelivered` 의 `deliveredAt: nowIso() as unknown as Date` + line 368 `markReportConsumed` 의 `consumedAt: nowIso() as unknown as Date` — `as unknown as Date` 타입 cast 는 거짓말이고 런타임은 string 이라 동일 에러. NPC 보고 delivery / 사용자 consume 흐름 양쪽이 unhandled rejection 으로 뜸 | 3 개 call site 모두 `new Date()` 로 변경. 라이브 + 소스 양쪽 적용. **추가**: `deskrpg-master/` 를 별도 GitHub repo (`https://github.com/ChoonwooLim/deskrpg` private) 로 분리 — TwinverseAI 의 `.gitignore` 대상이라 영구 보존이 안 되던 문제 해결. 향후 fix 는 git commit + push 로 영구화 | `deskrpg-master/src/lib/task-reporting.ts` (3 hunks), `https://github.com/ChoonwooLim/deskrpg` |
| 2026-05-01 | TwinverseAI 어드민 OpenClaw 콘솔 chat 에서 NPC 가 보낸 마크다운 링크/이미지가 plain text 로만 보여 클릭 불가 + 컨테이너 내부 경로 (`/data/.openclaw/...`) 노출 | [ChatTab.jsx:173] 가 `<div>{message.content}</div>` 로 plain text 렌더링. `react-markdown` 은 설치돼 있는데 사용 안 함. 추가로 NPC 가 `/__openclaw__/canvas/...` 같은 root-relative URL 을 주는데 페이지 origin 이 `twinverseai.twinverse.org` 라 404 | (1) assistant 메시지를 `<ReactMarkdown>` 으로 감싸 링크/볼드/코드 렌더링, (2) `resolveOpenClawHref` 가 `/__openclaw__/*` 로 시작하는 href 를 `https://openclaw.twinverse.org` 로 prepend, (3) target=_blank+rel=noopener 자동 부여, (4) 17 개 OpenClaw 에이전트 IDENTITY.md 에 운영 가이드 append — "절대 컨테이너 내부 경로 노출 X, 항상 `https://openclaw.twinverse.org/__openclaw__/...` 절대 URL 사용" + "태스크는 사용자가 명시적으로 '등록해줘' + 승인 카드 클릭해야 등록됨, '등록 완료' 환각 금지" | `frontend/src/pages/admin/openclaw/ChatTab.jsx`, twinverse-ai openclaw 컨테이너의 `/data/.openclaw/workspace-*/IDENTITY.md` 17 개 |
| 2026-05-05 | NewsActionModal 의 두 슬래시 명령 (`/plugin marketplace add` + `/plugin install`) 을 한 번에 paste → Claude Code 가 한 명령으로 합쳐 처리 → Windows 경로에 줄바꿈/공백 침입 → `Failed to clone marketplace repository: Invalid argument` (claude-mem 첫 설치 시도 시 실패) | 모달이 두 명령을 단일 코드블록 + 단일 [복사] 버튼으로 노출 → 사용자가 두 줄을 동시에 클립보드에 담아 paste. Claude Code 의 슬래시 파서가 두 줄을 단일 인자로 처리 | `commandBlocks` 배열 도입, 각 명령이 독립 카드 (`1️⃣ 마켓플레이스 추가` / `2️⃣ 플러그인 설치 (위 성공 후)`) + 자체 [복사] 버튼. 한 번에 복사할 수 없는 구조로 회귀 차단. `secondaryNote` 에 "한 줄씩 따로 입력" 명시 | `frontend/src/pages/admin/NewsActionModal.{jsx,module.css}` |
